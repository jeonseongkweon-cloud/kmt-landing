# 계명태권도 CLASS v0.9.1 · WORK9

## 목적

동일한 보호자 전화번호를 사용하는 형제·쌍둥이 원생을 이름이나 전화번호가 아닌 고유 원생 ID로 구분합니다.

## 적용 순서

1. 휴대폰 문자 발신기를 OFF로 유지합니다.
2. Supabase SQL Editor에서 `202608300910_11_twin_identity_guard_v091.sql` 전체를 한 번 실행합니다.
3. ZIP의 `class` 폴더를 홈페이지 최상위 폴더에 덮어씁니다.
4. GitHub Pages 배포 완료 후 출석 화면을 새로고침합니다.
5. 화면 버전이 `v0.9.1 · 쌍둥이 식별 패치`인지 확인합니다.

## 동작 확인

- 김나라 또는 김우리처럼 같은 보호자 번호를 쓰는 원생의 출석/지각을 누르면 이름과 원생번호를 확인하는 창이 표시됩니다.
- 확인한 학생의 고유 ID와 보호자 소유 ID가 다르면 문자대기 생성을 서버에서 차단합니다.
- SQL 실행 결과의 `identity_check`가 `정상`이면 해당 과거 기록의 ID 연결은 정상입니다.

## 안전 시험

실제 보호자 번호를 바꾸기 전에 문자 발신기를 OFF로 두고 문자대기 화면에서 학생 이름과 원생번호를 확인합니다. 시험 수신번호가 본인 번호인 것이 확인된 뒤에만 발신기를 ON으로 전환합니다.
